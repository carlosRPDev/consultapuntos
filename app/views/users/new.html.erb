<div class="container p-3">
  <div class="row justify-content-center p-3">
    <div class="col-sm-6 shadow border rounded">
      <h1>Crear Cuenta</h1>

      <%= form_for @user do |f| %>
        <div class="form-group">
          <%= f.text_field :cuenta, class: "form-control input-lg", placeholder: "Cuenta" %>
        </div>

        <div class="form-group">
          <%= f.password_field :password, class: "form-control input-lg", placeholder: "Contraseña" %>
        </div>

        <div class="form-group">
          <%= f.text_field :name, class: "form-control input-lg", placeholder: "Nombre" %>
        </div>

        <div class="actions">
          <%= f.submit class: "btn btn-danger btn-block btn-lg my-3" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
