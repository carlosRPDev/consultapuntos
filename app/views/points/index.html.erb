<div class="container">
  <div class="row">
    <div class="col-sm-9">
      <h1>Puntos de ventas</h1>

      <table class="table table-hover table-sm mt-3">
          <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Centro de Costo</th>
            <th>Tecnologia</th>
            <th>Observacion</th>
            <th>Ip Radio</th>
            <th>Ip Telefono</th>
            <th>Ip Computador</th>
          </tr>
        <% @point.each do |point| %>
        <tr>
          <td><%= point.id %></td>
          <td><%= point.name %></td>
          <td><%= point.ccosto %></td>
          <td><%= point.tecnologia %></td>
          <td><%= point.observacion %></td>
          <td><%= point.ipradio %></td>
          <td><%= point.iptele %></td>
          <td><%= point.ipcom %></td>
        </tr>
        <% end %>
      </table>
    </div>
    <div class="col-sm-3">
      <form>
        <div class="input-group input-group-sm mt-3">
          <input type="text" class="form-control" placeholder="Buscar punto de venta">
          <div class="input-group-append">
            <span class="input-group-text"><i class="fas fa-search"></i></span>
          </div>
        </div>
      </form>
      <div class="container d-flex justify-content-end m-3">
        <%= link_to "Nuevo punto de venta", new_point_path, class: "btn btn-danger" %>
      </div>
    </div>

  </div>
</div>
